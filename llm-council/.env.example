# LLM Council configuration — copy to `.env` and fill in your key.
# `.env` is git-ignored (see .gitignore); never commit real keys.

# OpenRouter API key. Get one at https://openrouter.ai/ (Settings → Keys)
# and make sure your account has credits / automatic top-up enabled.
OPENROUTER_API_KEY=sk-or-v1-...
